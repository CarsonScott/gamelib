from distutils.core import setup
import setuptools

setup(
	name='gamelib',
	version='1.0',
	description='Additional tools and classes that compliment the pygame library.',
	author='Carson Scott',
	packages=setuptools.find_packages(),
)